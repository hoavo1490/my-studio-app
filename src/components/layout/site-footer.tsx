export function SiteFooter() {
  return (
    <footer className="border-t border-[var(--card-border)] bg-[var(--bg-secondary)]">
      <div className="mx-auto max-w-5xl px-6 py-10 text-sm text-[var(--text-secondary)]">
        duskroom is a moody, artist-owned studio platform for showing work without algorithms.
      </div>
    </footer>
  );
}
